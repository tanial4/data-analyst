# Pinned to known-good, CVE-clean floors (verified with `pip-audit`).
# Re-run `pip-audit` after changing any of these.
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
openpyxl>=3.1.5
tabulate>=0.9
gradio>=6.16              # 6.x clears the CVEs that affected the 4.x line
pillow>=12.0              # image CVE fixes (also pulled in by gradio)
langchain-groq>=1.1
langchain-core>=1.0
pydantic>=2.5
pydantic-settings>=2.1

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
openpyxl>=3.1
tabulate>=0.9
gradio>=4.44,<5
langchain-groq>=0.2
langchain-core>=0.3
pydantic>=2.5
pydantic-settings>=2.1

# Transitive pins that the resolver otherwise gets wrong for the Gradio 4.x line:
huggingface_hub<1.0   # gradio 4.x imports HfFolder, removed in hub 1.0
websockets<13         # required by gradio-client 1.x
